{
  "name": "web-app-docker",
  "version": "1.0.0",
  "description": "this is used to test docker image creation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sparsh",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
